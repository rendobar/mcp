name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to publish (e.g. v1.0.0). Required only when run manually."
        required: true
        type: string

# v1.0.0 bootstrap uses NPM_TOKEN. Once the package exists on npm, register a
# Trusted Publisher at npmjs.com → @rendobar/mcp → Settings → Trusted Publisher
# (repo: rendobar/mcp, workflow: release.yml). Then remove NODE_AUTH_TOKEN and
# add `id-token: write` to switch to OIDC. See rendobar/rendobar publish-npm.yml
# for the OIDC flow.

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # for GitHub release notes (release-please attaches)
      id-token: write   # OIDC for npm provenance (required even with token auth)
      issues: write     # rollback step opens an issue on failure
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with: { version: 9 }

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      - run: pnpm test
      - run: pnpm build

      - name: Publish to npm with provenance
        run: |
          # Retry up to 3 times for transient registry blips.
          for i in 1 2 3; do
            if pnpm publish --provenance --access public --no-git-checks; then
              exit 0
            fi
            echo "Publish attempt $i failed, retrying in 30s..."
            sleep 30
          done
          echo "::error::Publish failed after 3 attempts"
          exit 1
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Smoke test (npx -y @rendobar/mcp@<tag> --version)
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          PKG="@rendobar/mcp@${VERSION}"
          # npm CDN propagation backoff: 30s, 60s, 90s, 120s, 150s
          for i in 1 2 3 4 5; do
            sleep $((i * 30))
            echo "Attempt $i: npx -y $PKG --version"
            OUTPUT=$(npx -y "$PKG" --version 2>&1) || OUTPUT=""
            if [ "$OUTPUT" = "$VERSION" ]; then
              echo "Smoke OK: $OUTPUT"
              exit 0
            fi
            echo "Got: '$OUTPUT' (expected: '$VERSION')"
          done
          echo "::error::Smoke test failed after 5 attempts"
          exit 1

      - name: Publish to MCP official registry
        # Best-effort — failure here doesn't roll back npm publish.
        # If this step fails, fix forward: re-run the workflow or run mcp-publisher manually.
        continue-on-error: true
        run: |
          npm install -g @modelcontextprotocol/mcp-publisher
          mcp-publisher publish --file server.json || echo "::warning::MCP registry publish failed — fix forward"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  rollback-on-failure:
    needs: publish
    if: failure()
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: Open issue for failed release
        run: |
          gh issue create \
            --title "Release ${GITHUB_REF_NAME} failed" \
            --body "The release workflow failed. If npm publish succeeded but a downstream step (smoke / registry) failed, the package is already live — fix forward via a follow-up workflow re-run rather than rolling back. If npm publish itself failed, deprecate any partial version with: \`npm deprecate @rendobar/mcp@${GITHUB_REF_NAME#v} \"do not use\"\`." \
            --repo "${{ github.repository }}"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
